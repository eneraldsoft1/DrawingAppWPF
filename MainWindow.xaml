<Window x:Class="DrawingAppWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:DrawingAppWPF"
        mc:Ignorable="d"
        Title="Drawing App" Height="700" Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель инструментов -->
        <ToolBar Grid.Row="0" Background="#FFF0F0F0" Margin="5" Padding="5">
            <Button x:Name="btnColorPicker" Click="ColorPicker_Click" ToolTip="Select color" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <Rectangle x:Name="currentColorRect" Width="24" Height="24" Stroke="Black" StrokeThickness="1" Margin="0,0,5,0"/>
                    <TextBlock Text="Color" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Separator Margin="5,0"/>

            <StackPanel Orientation="Horizontal" Margin="5,0" VerticalAlignment="Center">
                <TextBlock Text="Thickness:" Margin="0,0,5,0" VerticalAlignment="Center"/>
                <Slider x:Name="thicknessSlider" Minimum="1" Maximum="50" Value="5" Width="100"
                        ValueChanged="ThicknessSlider_ValueChanged"/>
                <TextBlock Text="{Binding Value, ElementName=thicknessSlider, StringFormat={}{0:F0}}" 
                           Width="30" Margin="5,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <Separator Margin="10,0"/>

            <ComboBox x:Name="shapeComboBox" Width="120" Margin="5" SelectionChanged="ShapeComboBox_SelectionChanged">
                <ComboBoxItem Content="Freehand"/>
                <ComboBoxItem Content="Line"/>
                <ComboBoxItem Content="Rectangle"/>
                <ComboBoxItem Content="Ellipse"/>
            </ComboBox>

            <Separator Margin="10,0"/>

            <Button Content="Clear" Click="Clear_Click" Margin="5" Padding="10,3"/>
            <Button Content="Save" Click="Save_Click" Margin="5" Padding="10,3"/>
        </ToolBar>

        <!-- Холст для рисования -->
        <InkCanvas x:Name="drawingCanvas" Grid.Row="1" Background="White" EditingMode="Ink"/>
    </Grid>
</Window>